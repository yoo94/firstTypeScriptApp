//함수의 파라미터의 타입을 정의
function sum(a: number,b: number){
    return a+b;
}

sum(10,20);

// 함수의 반환값에 타입을 정의
function add(): number {
    return 10;
}

// 함수에 타입을 정의
function hap(a: number, b: number): number{
    return a+b;
}